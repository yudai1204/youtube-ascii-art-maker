# YouTube Ascii Art Generator

require: Python >= 3.9

First, install libraries.

```shell
$ pip install -r requirements.txt
```

Then, set image file to root directory as `input.png`.

Finally, run `main.py`

```shell
$ python -m main
```
